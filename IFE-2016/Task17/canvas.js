cav = document.getElementById('cav')
if (cav.getContext) {
    var ctx = cav.getContext('2d');
}



// ctx.beginPath();
// ctx.arc(60, 60, 50, 0, Math.PI * 2, true);
// ctx.fillStyle = "#000000";
// ctx.fill();

// ctx.beginPath();
// ctx.arc(60, 60, 50, 0, Math.PI * 2, true);
// ctx.lineWidth = 1.0;
// ctx.strokeStyle = "#000";//描述画笔的样式或者颜色
// ctx.stroke();



// var myGradient = ctx.createLinearGradient(0, 0, 0, 160);//先设计出一个样式 起点的x,y 终点的x,y
// myGradient.addColorStop(0, "pink");//offset 0-1之间的值
// myGradient.addColorStop(1, "#636363");


// ctx.shadowOffsetX = 10; // 设置水平位移
// ctx.shadowOffsetY = 10; // 设置垂直位移
// ctx.shadowBlur = 5; // 设置模糊度
// ctx.shadowColor = "rgba(0,0,0,0.5)"; // 设置阴影颜色
// //shadow必须设置在图形的上方

// ctx.fillStyle = myGradient;//fillStyle描述颜色或者pattern（重复的图像等）
// // ctx.fillStyle = "#CC0000"
// ctx.fillRect(10, 10, 200, 100);



